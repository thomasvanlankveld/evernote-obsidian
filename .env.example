# Optional local defaults (copy to `.env`, gitignored). This repo no longer uses Evernote API env vars.

# Optional: path to your local Obsidian vault. CLI `index` default is `./data`.
# VAULT_PATH=./data

# `snapshot` takes the evernote-backup database path as `--db` (see README).
