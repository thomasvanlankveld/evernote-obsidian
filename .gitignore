# Local vault copy / imports (personal notes; large; not for the repo)
/data/

# Dependencies
node_modules/

# Local env files (CLI does not load them; kept gitignored if you use them for tooling)
.env
.env.*

# Local shell hooks (optional direnv; not tracked)
.envrc

# Logs and OS noise
*.log
.DS_Store

# Build / tool output (add paths here as the project grows)
/dist/
/out/
